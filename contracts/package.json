{
  "name": "contracts",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts",
    "deploy-local": "npx hardhat run scripts/deploy.ts --network localhost",
    "deploy-scroll": "npx hardhat run scripts/deploy.ts --network scrollSepolia",
    "deploy-alfajores": "npx hardhat run scripts/deploy.ts --network alfajores",
    "deploy-x1": "npx hardhat run scripts/deploy.ts --network x1",
    "deploy-zkevmtest": "npx hardhat run scripts/deploy.ts --network polygonZkEvmTestnet",
    "local-node": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.19.2"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@chainlink/contracts-ccip": "^1.2.1",
    "@nomicfoundation/hardhat-verify": "^2.0.2",
    "@openzeppelin/contracts": "^5.0.1",
    "@openzeppelin/contracts-upgradeable": "^5.0.1",
    "@safe-global/safe-core-protocol": "^0.2.1-alpha.0",
    "@zetachain/networks": "^2.5.0",
    "@zetachain/protocol-contracts": "^3.0.1",
    "@zetachain/toolkit": "^4.0.0"
  }
}
